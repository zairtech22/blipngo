# Copy this to .env and fill in your database credentials.
# Example Postgres connection string (change user/password/host/port/dbname):
# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/blipngo?schema=public"

# If you use a hosted DB (e.g. Supabase, Neon), paste the provided connection string here.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
