<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title><%= biz.name %> — AI Review Poster</title>
<style>
  :root{
    --ink:#0A0A0A;
    --muted:#6b7280;
    --border:#e5e7eb;
    --paper:#ffffff;
    --brand:<%= biz.brandColor || '#111827' %>;
    --cta-bg:<%= biz.ctaBgColor || '#0A0A0A' %>;
    --cta-fg:<%= biz.ctaColor   || '#EDC400' %>;
  }
  *{box-sizing:border-box}
  html,body{height:100%}
  body{margin:0;background:#f3f4f6;color:var(--ink);font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif}

  /* A4-ish poster canvas */
  .sheet{
    width: 794px;               /* A4 @96dpi */
    min-height: 1123px;         /* A4 height */
    margin: 16px auto;
    background: var(--paper);
    border: 1px solid var(--border);
    border-radius: 14px;
    overflow: hidden;
    display: grid;
    grid-template-rows: auto 1fr auto;
  }

  /* Make layout similar to single-QR poster: centered header, large logo/title and big CTA */
  .head{
    padding: 20px;
    border-bottom: 1px solid var(--border);
    display: flex;
    flex-direction: column;
    gap: 10px;
    align-items: center;
    text-align: center;
  }
  .logo{
    width:120px;height:120px;border-radius:20px;
    background:<%= biz.logoBgColor || '#0A0A0A' %>;
    display:grid;place-items:center;overflow:hidden;border:4px solid var(--ink);
  }
  .logo img{max-width:90%;max-height:90%;object-fit:contain;display:block}
  .title{font-size:clamp(28px,4.6vw,44px);font-weight:900;line-height:1}
  .subtitle{color:var(--muted);font-weight:600}

  .cta{
    margin: 10px auto 18px;
    background: var(--cta-bg);
    color: var(--cta-fg);
    padding: 14px 18px;
    border-radius: 12px;
    font-weight: 900;
    font-size: clamp(20px,3.6vw,32px);
    max-width: 600px;
    text-align: center;
    text-transform: uppercase;
    box-shadow: 0 4px 10px rgba(0,0,0,0.12);
  }
  /* Welcoming tweaks */
  .welcome {
    font-size: 15px;
    color: var(--muted);
    margin-top: 6px;
    font-weight: 600;
  }
  .panel{box-shadow: 0 12px 30px rgba(16,24,40,0.08)}

  .body{
    padding: 22px 20px 28px;
    display:grid;
    grid-template-rows:auto 1fr;
    gap: 18px;
  }

  .panel{
    border-radius: 16px;
    background: var(--brand);
    color: #fff;
    display:flex;
    flex-direction:column;
    align-items:center;
    justify-content:center;
    padding: 22px;
    min-height: 520px;
    text-align:center;
  }
  .panel h2{margin:8px 0 10px 0;font-size:24px;font-weight:900}
  .panel p{margin:0 0 8px 0;font-weight:600}
  .qr{
    background:#fff;border-radius:14px;padding:18px;margin-top:14px;border:1px solid rgba(0,0,0,0.12);display:flex;align-items:center;justify-content:center
  }
  /* larger QR to match poster single-qr size */
  .qr img{width:320px;height:320px;display:block}

  .tip{font-size:13px;opacity:.95;margin-top:10px}

  .foot{
    padding: 12px 20px;
    border-top: 1px solid var(--border);
    color: var(--muted);
    font-size: 12px;
    display:flex;
    justify-content:space-between;
    align-items:center;
  }

  @media print{
    body{background:#fff}
    .sheet{margin:0;border:none;border-radius:0;width:auto;min-height:auto}
  }
</style>
</head>
<body>
  <div class="sheet">
    <div class="head">
      <% if (biz.showLogo && biz.logoUrl) { %>
        <div class="logo"><img src="<%= biz.logoUrl %>" alt="Logo"/></div>
      <% } %>
      <div>
        <div class="title"><%= biz.name %></div>
        <div class="subtitle">Scan to write a <%= platform %> review in seconds</div>
        <div class="welcome">Welcome — we'll help you craft a friendly, genuine review in seconds ✨</div>
      </div>
    </div>

    <!-- CTA bar (uses business colors) -->
      <div class="cta">
        <%= (typeof headline!=='undefined' && headline) ? headline : ('We’ll draft a short, friendly review — edit & post to '+platform+' — it only takes a minute!') %>
      </div>

    <div class="body">
      <div class="panel">
  <h2>AI Review Assistant ✨</h2>
  <p>Open the assistant to get a short draft you can tweak, copy, and post in moments.</p>
        <div class="qr">
          <img src="/qr/ai/<%= biz.slug %>/<%= platform %>.png" alt="AI Review QR"/>
        </div>
  <div class="tip">Point your camera at the QR to start — we're here to make it quick and easy.</div>
      </div>
    </div>

    <div class="foot">
      <div><%= biz.publicFooter || 'Thanks for supporting local!' %></div>
      <!-- <div><%= platform %> reviews</div> -->
    </div>
  </div>
</body>
</html>
