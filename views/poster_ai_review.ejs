<!DOCTYPE html>
<html><head><meta charset="utf-8"/><meta name="viewport" content="width=device-width,initial-scale=1"/>
<title><%= biz.name %> — AI Review Poster</title>
<style>
  body{margin:0;background:#f1f5f9;font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif}
  .sheet{width:794px;min-height:1123px;margin:12px auto;background:#fff;border:1px solid #e5e7eb;border-radius:12px;overflow:hidden;display:grid;grid-template-rows:auto 1fr auto}
  .head{padding:20px;border-bottom:1px solid #e5e7eb;display:flex;gap:12px;align-items:center}
  .logo{width:64px;height:64px;border-radius:12px;background:<%= biz.logoBgColor || '#0A0A0A' %>;display:grid;place-items:center;overflow:hidden}
  .logo img{max-width:100%;max-height:100%;object-fit:contain}
  .ttl{font-size:28px;font-weight:900}
  .sub{color:#6b7280;font-weight:600}
  .body{padding:24px}
  .cta{margin:10px 0 16px;font-weight:800;padding:12px 14px;border-radius:10px;color:<%= biz.ctaColor || '#EDC400' %>;background:<%= biz.ctaBgColor || '#0A0A0A' %>}
  .panel{border:2px solid #111;border-radius:16px;padding:16px;background:<%= biz.brandColor || '#111827' %>;color:#fff;min-height:520px;display:grid;grid-template-rows:auto 1fr auto;justify-items:center}
  .big{font-size:20px;font-weight:900}
  .qr{background:#fff;padding:12px;border-radius:12px;margin-top:12px}
  .qr img{width:240px;height:240px}
  .foot{padding:12px 20px;border-top:1px solid #e5e7eb;font-size:12px;color:#6b7280;display:flex;justify-content:space-between}
</style></head>
<body>
  <div class="sheet">
    <div class="head">
      <% if (biz.showLogo && biz.logoUrl) { %><div class="logo"><img src="<%= biz.logoUrl %>"/></div><% } %>
      <div>
        <div class="ttl"><%= biz.name %></div>
        <div class="sub">Scan to write a <%= platform %> review in seconds</div>
      </div>
    </div>
    <div class="body">
      <div class="cta">We’ll draft it for you — you can edit & copy before posting.</div>
      <div class="panel">
        <div class="big">Open AI Review Helper</div>
        <div class="qr">
          <img src="/qr/ai/<%= biz.slug %>/<%= platform %>.png" alt="QR"/>
        </div>
        <div>Point your camera at the QR to begin</div>
      </div>
    </div>
    <div class="foot">
      <div><%= biz.publicFooter || 'Thanks for supporting local!' %></div>
      <div><%= platform %> reviews</div>
    </div>
  </div>
</body></html>
