<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title><%= biz.name %> — AI Review Poster</title>
<style>
  :root{
    --ink:#0A0A0A;
    --muted:#6b7280;
    --border:#e5e7eb;
    --paper:#ffffff;
    --brand:<%= biz.brandColor || '#111827' %>;
    --cta-bg:<%= biz.ctaBgColor || '#0A0A0A' %>;
    --cta-fg:<%= biz.ctaColor   || '#EDC400' %>;
  }
  *{box-sizing:border-box}
  html,body{height:100%}
  body{margin:0;background:#f3f4f6;color:var(--ink);font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif}

  /* A4-ish poster canvas */
  .sheet{
    width: 794px;               /* A4 @96dpi */
    min-height: 1123px;         /* A4 height */
    margin: 16px auto;
    background: var(--paper);
    border: 1px solid var(--border);
    border-radius: 14px;
    overflow: hidden;
    display: grid;
    grid-template-rows: auto 1fr auto;
  }

  .head{
    padding: 20px;
    border-bottom: 1px solid var(--border);
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 12px;
    align-items: center;
  }
  .logo{
    width:72px;height:72px;border-radius:14px;
    background:<%= biz.logoBgColor || '#0A0A0A' %>;
    display:grid;place-items:center;overflow:hidden
  }
  .logo img{max-width:100%;max-height:100%;object-fit:contain}
  .title{font-size:30px;font-weight:900;line-height:1.1}
  .subtitle{color:var(--muted);font-weight:600}

  .cta{
    margin: 16px 20px 0;
    background: var(--cta-bg);
    color: var(--cta-fg);
    padding: 14px 16px;
    border-radius: 12px;
    font-weight: 900;
  }

  .body{
    padding: 22px 20px 28px;
    display:grid;
    grid-template-rows:auto 1fr;
    gap: 18px;
  }

  .panel{
    border: 2px solid #111;
    border-radius: 16px;
    background: var(--brand);
    color: #fff;
    display:grid;
    grid-template-rows: auto 1fr auto;
    justify-items:center;
    align-content:start;
    padding: 18px;
    min-height: 560px;
    text-align:center;
  }
  .panel h2{margin:8px 0 10px 0;font-size:22px;font-weight:900}
  .panel p{margin:0 0 8px 0;font-weight:600}
  .qr{
    background:#fff;border-radius:14px;padding:14px;margin-top:12px;border:1px solid #111;
  }
  .qr img{width:260px;height:260px;display:block}

  .tip{font-size:13px;opacity:.95;margin-top:10px}

  .foot{
    padding: 12px 20px;
    border-top: 1px solid var(--border);
    color: var(--muted);
    font-size: 12px;
    display:flex;
    justify-content:space-between;
    align-items:center;
  }

  @media print{
    body{background:#fff}
    .sheet{margin:0;border:none;border-radius:0;width:auto;min-height:auto}
  }
</style>
</head>
<body>
  <div class="sheet">
    <div class="head">
      <% if (biz.showLogo && biz.logoUrl) { %>
        <div class="logo"><img src="<%= biz.logoUrl %>" alt="Logo"/></div>
      <% } %>
      <div>
        <div class="title"><%= biz.name %></div>
        <div class="subtitle">Scan to write a <%= platform %> review in seconds</div>
      </div>
    </div>

    <!-- CTA bar (uses business colors) -->
    <div class="cta">
      <%= (typeof headline!=='undefined' && headline) ? headline : 'We’ll draft it for you — edit & paste, then post on '+platform+'.' %>
    </div>

    <div class="body">
      <div class="panel">
        <h2>AI Review Helper</h2>
        <p>Open the helper, get a ready draft, edit, copy & post.</p>
        <div class="qr">
          <img src="/qr/ai/<%= biz.slug %>/<%= platform %>.png" alt="AI Review QR"/>
        </div>
        <div class="tip">Point your camera at the QR to start</div>
      </div>
    </div>

    <div class="foot">
      <div><%= biz.publicFooter || 'Thanks for supporting local!' %></div>
      <div><%= platform %> reviews</div>
    </div>
  </div>
</body>
</html>
