<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title><%= biz.name %> — Quick Review</title>
  <style>
    :root{
      --bg: <%= biz.brandColor ? biz.brandColor : '#FFD21E' %>;
      --ink:#0A0A0A; --paper:#FFFBEA; --muted:#646464;
      --logo-bg:<%= biz.logoBgColor ? biz.logoBgColor : '#0A0A0A' %>;
      --cta-color:<%= biz.ctaColor ? biz.ctaColor : '#EDC400' %>;
      --cta-bg:<%= biz.ctaBgColor ? biz.ctaBgColor : '#0A0A0A' %>;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif;
      background: var(--bg);
      color: var(--ink);
      -webkit-font-smoothing:antialiased; -moz-osx-font-smoothing:grayscale;
    }
    .page{
      min-height:100vh; display:flex; justify-content:center; padding:16px;
      <% if (embed) { %> background: transparent; <% } %>
    }
    .poster{
      width:100%; max-width:820px; margin:auto; background:var(--paper);
      border-radius:28px; padding:32px 28px; box-shadow:0 10px 24px rgba(0,0,0,0.1);
    }
    .header{text-align:center; margin-bottom:24px}
    .logo{
      width:120px; height:120px; margin:0 auto 10px;
      border:4px solid var(--ink); border-radius:20px; background:var(--logo-bg);
      display:flex; align-items:center; justify-content:center; overflow:hidden;
    }
    .logo img{max-width:85%; max-height:85%; width:auto; height:auto; object-fit:contain}
    .logo div{color:var(--paper); font-weight:800; font-size:16px; line-height:1.3; text-align:center}
    h1.biz{margin:0; font-weight:900; font-size:clamp(28px,4.6vw,44px)}
    p.tag{margin:6px 0 0; color:var(--muted); font-weight:600; font-size:clamp(14px,2.2vw,18px)}
    p.welcome{margin:8px 0 0; color:var(--muted); font-weight:600; font-size:clamp(13px,2vw,15px)}
    .cta{
      margin:20px auto; padding:12px 20px; text-align:center;
      background:var(--cta-bg); color:var(--cta-color);
      border-radius:12px; font-weight:900; font-size:clamp(22px,3.8vw,34px); text-transform:uppercase;
      box-shadow:0 4px 10px rgba(0,0,0,0.2); max-width:640px;
    }
    .qr-section{
      background: var(--bg); border:4px solid var(--ink); border-radius:18px;
      padding:24px; text-align:center; margin:22px auto; max-width:640px;
    }
    .qr-section h2{margin:0 0 10px; font-size:24px; font-weight:900}
    .qr-section p{margin:0 0 18px; font-weight:600}
    .qr-box{background:#fff; border:3px solid var(--ink); border-radius:12px; padding:20px; width:fit-content; margin:0 auto}
    .qr-box img{width:280px; height:280px; display:block}
    .tip{margin-top:12px; font-size:13px; font-weight:600}
    .fineprint{margin-top:16px; text-align:center; font-size:12px; color:var(--muted); font-weight:600}
    @media print{
      @page{margin:0}
      html,body{height:auto}
      body{background:#fff !important; color:#000 !important; -webkit-print-color-adjust:exact; print-color-adjust:exact}
      .poster{width:186mm; height:273mm; margin:12mm auto; padding:8mm; border-width:3px; box-sizing:border-box}
      .logo{width:30mm; height:30mm; border-width:2px; border-radius:6mm; margin-bottom:4mm}
      h1.biz{font-size:24pt}
      .cta{font-size:18pt; padding:5mm 8mm; margin:4mm auto 8mm; max-width:140mm}
      .qr-section{padding:6mm; border-width:2px}
      .qr-box{padding:4mm; border-width:2px}
      .qr-box img{width:60mm; height:60mm}
      .fineprint{font-size:9.5pt; margin-top:6mm}
    }
  </style>
</head>
<body>
  <div class="page">
    <main class="poster" aria-label="AI Review Poster">
      <header class="header">
        <div class="logo" aria-hidden="true">
          <% if (biz.logoUrl) { %>
            <img src="<%= biz.logoUrl %>" alt="<%= biz.name %> logo"/>
          <% } else { %>
            <div><%= biz.name %></div>
          <% } %>
        </div>
        <h1 class="biz"><%= biz.name %></h1>
        <% if (biz.publicSubtitle) { %><p class="tag"><%= biz.publicSubtitle %></p><% } %>
        <% if (welcome) { %><p class="welcome"><%= welcome %></p><% } %>
      </header>

      <section class="cta"><%= headline || 'Share your experience in seconds' %></section>

      <section class="qr-section" aria-label="QR Section">
        <h2><%= qrTitle || 'AI Review Assistant ✨' %></h2>
        <p><%= qrDescription || 'Open the assistant to get a short draft you can tweak, copy, and post in moments.' %></p>
        <div class="qr-box">
          <img
            src="/qr/ai/<%= biz.slug %>/<%= platform %>.png?ts=<%= Date.now() %>"
            alt="QR code to open AI review assistant for <%= biz.name %> on <%= platform %>"/>
        </div>
        <div class="tip"><%= qrTip || 'Point your camera at the QR to start — we’ll make it quick and easy.' %></div>
      </section>

      <p class="fineprint"><%= publicFooter || 'Thanks for supporting local!' %></p>
    </main>
  </div>
</body>
</html>
